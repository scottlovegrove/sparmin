# --- Node / workspace tooling ---
node_modules/
*.log

# --- Astro (apps/marketing) ---
dist/
.astro/

# --- Cloudflare Workers (apps/companion) ---
.wrangler/
# Local secrets for `wrangler dev`. Never commit — this repo is public.
.dev.vars
.dev.vars.*
!.dev.vars.example
# ...but the SOPS-encrypted secrets ARE committed (safe — only values encrypted).
!.dev.vars.sops

# --- VS Code: ignore local machine settings, keep shared recommendations ---
.vscode/*
!.vscode/extensions.json

# --- Browser automation scratch (snapshots, screenshots) ---
.playwright-cli/

# --- OS cruft ---
.DS_Store
Thumbs.db
