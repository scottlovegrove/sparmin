// D1 (SQLite) schema, Drizzle dialect. Tables — stations, sessions,
// station_intervals — land with the schema work; this file exists so the
// Drizzle config and `db:generate` wiring are in place from the scaffold.
export {}
