# @sparmin/web

Single-page marketing website for Sparmin — what the app is, how to use it, and
where to install it from the Connect IQ Store.

**Not built yet.** This directory is a placeholder that reserves the `apps/web`
workspace. The site's toolchain (framework, build, deploy) will be chosen when
the site is created.
